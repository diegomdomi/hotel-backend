CREATE TABLE Users(idUsers INT NOT NULL AUTO_INCREMENT,
				   first_name VARCHAR (50),
				   job_desk VARCHAR (50),
				   schedules DATE,
				   contact VARCHAR (50),
                   PRIMARY KEY (idUsers));